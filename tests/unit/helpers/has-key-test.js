
import { hasKey } from 'szglab5-frontend/helpers/has-key';
import { module, test } from 'qunit';

module('Unit | Helper | has key');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hasKey([42]);
  assert.ok(result);
});


`import { eq } from 'szglab5-frontend/helpers/eq'`
`import { module, test } from 'qunit'`

module 'Unit | Helper | eq'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = eq 42
  assert.ok result

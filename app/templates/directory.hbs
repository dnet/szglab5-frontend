<div class="content directory">
  {{left-menu title="Felhasználók" currentView=currentView goToView=(action 'goToView') subMenu=subMenu}}

  <div class="right">
    <div class="directory">
      {{#unless showSettings}}
        <div class="directory-search-fields">
          <div class="text label">Keresés</div>
          {{input type="text" value=search placeholder="Keresés"}}

          <button {{action 'showTable'}}>Keresés</button>
        </div>
        {{#if showTable}}
          <div class="directory-table">
            {{table-component header=header body=users rowIndecies=rowIndecies addClass='user-table' openSettings=(action "openSettings") withSettings=true withImpersonate=true}}
          </div>
          {{#if showNextPage}}
            <button {{action 'nextPage'}}>További betöltése</button>
          {{/if}}
        {{/if}}
      {{/unless}}
      {{#if showSettings}}
        {{directory-settings header=model.header userDetails=model.userDetails homeworkTypes=model.homeworkTypes closeSettings=(action "closeSettings")}}
      {{/if}}
    </div>
  </div>
</div>

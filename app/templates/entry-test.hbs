<div class="content entry-test">
  <div class="left">
    <div class="title">Beugrók</div>
    <div class="submenu">
      <ul>
        {{#each subMenu as |menu|}}
          <li>
            <a class="submenu-text {{if (eq menu.key currentView) 'active'}}" {{action 'goToView' menu.key}}>
              {{menu.description}}
            </a>
          </li>
        {{/each}}
      </ul>
    </div>
  </div>
  <div class="right">
    <div class="entry-test">
      {{#if (eq currentView 'creator')}}
        {{entry-test-creator}}
      {{else}}
        {{entry-test-view}}
      {{/if}}
    </div>
  </div>
</div>

{{#if errorMessage}}
  {{login-box goToSettings=(action 'goToSettings') redirect=redirect errorShibboleth=errorMessage}}
{{else}}
  {{login-box goToSettings=(action 'goToSettings') redirect=redirect }}
{{/if}}


{{redirectChanged}}

<div class="text label">Feladattípusok</div>
<div class="button-toggles">
  {{#each model.exerciseTypes as |exerciseType|}}
    <button type="button" class="btn--toggle {{if (has-key currentTypes exerciseType.name) 'active'}}" {{action 'toggleType' exerciseType}}>{{exerciseType.name}}</button>
  {{/each}}
</div>

{{#if Deliverable.isUploadable}}
  <div class="text label">Feltöltés {{Deliverable.DeliverableTemplate.description}}:</div>
  {{file-uploader-box url=Deliverable.uploadUrl started=(action 'uploadStarted') success=(action 'uploadSuccess') failed=(action 'uploadFailed')}}

{{#if Deliverable.uploaded}}
  <div class="text label">Letöltés {{Deliverable.DeliverableTemplate.description}}:</div>
  <a class="button" {{action 'download'}}>Download</a>
  <div class="text label">{{Deliverable.DeliverableTemplate.description}} Feltöltés ideje:</div>
  <div class="text">{{dateformat Deliverable.lastSubmittedDate}}</div>
{{/if}}

  {{#if success}}
    <div class="success-box">
      Sikeres feltöltés.
    </div>
  {{else}}
    {{#unless (eq error '')}}
      <div class="error-box">
        {{error}}
      </div>
    {{/unless}}
  {{/if}}
{{/if}}

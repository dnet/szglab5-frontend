<div class="entry-test-list">
  {{#unless showSettings}}
    {{object-table-component header=header body=getBody rowIndecies=rowIndecies withDelete=true withSettings=true openSettings=(action 'openSettings')}}
  {{/unless}}
  {{#if showSettings}}
    {{entry-test-creator entryTest=entryTest edit=true closeSettings=(action "closeSettings") model=model}}
  {{/if}}
</div>

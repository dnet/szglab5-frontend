<nav class="menu navbar navbar-default navbar-static-top">
  <!-- LEFT MENU ITEMS-->
  <!-- THESE THESE SHOULD BE REMOVED-->
  <div class="pull-xs-left">
    <div class="logo"><img src="assets/images/logo.png"></div>
    <div class="nav navbar-nav">
      <!-- USER ROLE CHOOSER-->
      <div class="btn-group menu-btn-group" role="group">
        <select class="role-selector" aria-labelledby="btnGroupDrop1" {{action 'changeUserRight' value="target.value" on='change'}}>
          {{#each userRightLabels as |userright|}}
          <option value={{userright}}>
              {{!-- <a class="navbar-btn dropdown-item menu-button" {{action 'changeUserRight' userright}}>{{userright}}</a> --}}
              {{userright}}
          </option>
          {{/each}}
        </select>
        </div>
        {{#each userRights as |right|}}
          {{#if (eq right 'admin')}}
            {{#each adminMenu as |menuitem|}}
              {{#link-to menuitem.key tagName='a' class="menu-text" }}<i class="fa fa-{{menuitem.icon}}" aria-hidden="true"></i> {{menuitem.value}}{{/link-to}}
            {{/each}}
          {{/if}}
          {{#if (eq right 'evaluator')}}
            {{#each evaluatorMenu as |menuitem|}}
              {{#link-to menuitem.key tagName='a' class="menu-text" }}<i class="fa fa-{{menuitem.icon}}" aria-hidden="true"></i> {{menuitem.value}}{{/link-to}}
            {{/each}}
          {{/if}}
          {{#if (eq right 'demonstrator')}}
            {{#each demonstratorMenu as |menuitem|}}
              {{#link-to menuitem.key tagName='a' class="menu-text" }}<i class="fa fa-{{menuitem.icon}}" aria-hidden="true"></i> {{menuitem.value}}{{/link-to}}
            {{/each}}
          {{/if}}
          {{#if (eq right 'student')}}
            {{#each studentMenu as |menuitem|}}
              {{#link-to 'student' tagName='a' class="menu-text" }}<i class="fa fa-{{menuitem.icon}}" aria-hidden="true"></i> {{menuitem.value}}{{/link-to}}
            {{/each}}
          {{/if}}
        {{/each}}
    </div>
  </div>



  <!-- RIGHT MENU ITEMS-->
  <div class="pull-xs-right ">
    <div class="menu-text user">{{username}}</div>
    <div class="nav navbar-nav">
      <!-- BUTTONS BASED ON USER ROLE-->
      <!-- BUTTONS EVERYONE SHOULD SEE-->
      {{#link-to 'settings' tagName='a' }} <i class="fa fa-cogs" aria-hidden="true"></i>{{/link-to}}
      <a> <i class="fa fa-sign-out" aria-hidden="true"></i></a>
    </div>
  </div>
</nav>
